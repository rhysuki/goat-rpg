-- base_timer.lua is vrld's hump timer.
-- timer.lua is a327ex's EnhancedTimer, which requires rxi's "classic."
-- all of this just so I can do require 'lib.timer' easily...

return require('lib.timer.timer')